export const goToHomePage = (history) => {
    history.push("/");
  };

export const goToTrip = (history) => {
    history.push("/trip");
  };

export const goToMembers = (history) => {
    history.push("/members");
  };
